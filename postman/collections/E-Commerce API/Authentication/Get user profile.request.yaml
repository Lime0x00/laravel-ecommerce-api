$kind: http-request
url: '{{baseUrl}}/auth/profile'
method: GET
headers:
  Accept: application/json
body:
  type: text
  content: ''
auth:
  type: bearer
  credentials:
    token: '{{bearerToken}}'
examples: ./.resources/Get user profile.resources/examples
order: 4000
